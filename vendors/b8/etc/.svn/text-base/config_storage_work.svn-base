############################################################
# Configuration file for b8's interface work storage class #
############################################################

### Use a MySQL table ###

# This is the name of the MySQL table used for storing the data
#tableName = heartstrings2007_lonelythinker_bayesian_wordlists

# If the interface should set up it's own MySQL connection, put in
# here your access data and remove the comments. If you also use MySQL as
# storage backend for b8, you can put this data into config_interface, so
# that the same connection can also be used by b8's storage backend

#host = 127.0.0.1
#user = root
#pass = 
#db   = b8